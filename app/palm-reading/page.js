'use client';
import { useState } from 'react';

export default function PalmReadingPage(){
 const [form,setForm]=useState({name:'',hand:'Right Hand',gender:'',dob:'',focus:'Love & Future'}); const [file,setFile]=useState(null); const [step,setStep]=useState(0); const progress=step===0?25:step===1?60:100;
 const update=e=>setForm({...form,[e.target.name]:e.target.value});
 return <main className="section"><div className="container"><div className="sectionTitle"><span className="kicker">Palmistry</span><h2>Free Palm Reading</h2><p>A guided palmistry flow with hand selection, personal details, palm photo upload, and result screen.</p></div><div className="formShell"><div className="formCard"><div className="progress"><span style={{width:`${progress}%`}} /></div><label>Name</label><input name="name" value={form.name} onChange={update} placeholder="Enter your name"/><label>Which palm?</label><select name="hand" value={form.hand} onChange={update}><option>Right Hand</option><option>Left Hand</option><option>Both Hands</option></select><label>Gender</label><select name="gender" value={form.gender} onChange={update}><option value="">Select</option><option>Female</option><option>Male</option><option>Other</option></select><label>Date of Birth</label><input type="date" name="dob" value={form.dob} onChange={update}/><label>Reading Focus</label><select name="focus" value={form.focus} onChange={update}><option>Love & Future</option><option>Career & Money</option><option>Life Path</option><option>Marriage & Family</option></select><label>Upload Palm Photo</label><input type="file" accept="image/*" onChange={e=>setFile(e.target.files?.[0]||null)}/><button className="cta" style={{marginTop:18,width:'100%'}} onClick={()=>setStep(2)}>Read My Palm</button></div><div className="resultBox"><h3>Palm Reading Result</h3>{step===2?<><p>{form.name||'Dear visitor'}, your {form.hand.toLowerCase()} shows a path connected with responsibility, intuition, and slow but meaningful progress. The strongest message around {form.focus.toLowerCase()} is that your future improves when you choose patience over pressure.</p><p>{file?`Palm image selected: ${file.name}. In a full production version, this image can be reviewed through a secure palm-reading engine before showing a deeper result.`:'You can still view a sample reading, but uploading a clear palm photo will make the flow feel complete.'}</p><div className="grid grid3" style={{marginTop:18}}><div className="resultCard"><h3>Heart Line</h3><p>Deep feelings, loyalty, and careful trust.</p></div><div className="resultCard"><h3>Head Line</h3><p>Practical thinking with strong intuition.</p></div><div className="resultCard"><h3>Life Line</h3><p>Gradual strength and steady personal growth.</p></div></div></>:<p>Complete the form and upload a palm photo to see the palmistry result here.</p>}</div></div></div></main>
}
