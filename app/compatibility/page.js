'use client';
import { useMemo, useState } from 'react';
import { signs } from '@/lib/astroData';
export default function CompatibilityPage(){const [a,setA]=useState('Aries');const [b,setB]=useState('Libra');const score=useMemo(()=>{const ia=signs.findIndex(s=>s.name===a), ib=signs.findIndex(s=>s.name===b); return 62+((ia+ib)*7%35)},[a,b]);return <main className="section"><div className="container"><div className="sectionTitle"><span className="kicker">Compatibility</span><h2>Zodiac Love Match</h2><p>Compare two signs for attraction, timing, trust, and communication.</p></div><div className="formShell"><div className="formCard"><label>Your Sign</label><select value={a} onChange={e=>setA(e.target.value)}>{signs.map(s=><option key={s.name}>{s.name}</option>)}</select><label>Partner Sign</label><select value={b} onChange={e=>setB(e.target.value)}>{signs.map(s=><option key={s.name}>{s.name}</option>)}</select></div><div className="resultBox"><h3>{a} + {b}</h3><p>This connection shows {score}% compatibility. The attraction is present, but the strongest growth comes through clear communication and respect for timing. One side may move faster while the other needs more space.</p><div className="progress"><span style={{width:`${score}%`}} /></div><div className="grid grid3" style={{marginTop:18}}><div className="resultCard"><h3>Love</h3><p>{score}%</p></div><div className="resultCard"><h3>Trust</h3><p>{Math.max(55,score-8)}%</p></div><div className="resultCard"><h3>Timing</h3><p>{Math.min(96,score+5)}%</p></div></div></div></div></div></main>}
